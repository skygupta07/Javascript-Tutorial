let a = 5;
let b = 8;

console.log ( " a + b = " , a + b );

let c = 9; // number
let d = "9";    // string 

console.log ( " is c == d ", c == d );      // true


// stricter version of comparison -> it returns true when both its value and its data 
// type is equal....

console.log( "is c === d" , c === d );  // false

// alert is one time pop up 
// prompt is used to take input from the user


    let num = prompt("enter the num");

    if (num % 5 == 0) alert(" number is multiple of 5 ");
    else alert ("not multiple of 5 ");

    num-- ;

 
